import styled from "styled-components";

const Title = styled.h1`
    text-align: center;
    font-family: 'Poppins';
`;

const StyledLink = styled.a`
    font-family: 'Poppins';
    font-size:16px;
    margin-top:6px;
    display: block;
    cursor: pointer;
`;

export { StyledLink, Title };
